# Extrair todo texto _()
pybabel extract -F babel.cfg -o messages.pot .

# Inicializar novo idioma
pybabel init -i messages.pot -d locales -l pt_BR

# Atualizar novas strings sem apagar o que já foi traduzido
pybabel update -i messages.pot -d locales

# Para que o streamlit veja as mudanças
pybabel compile -d locales
